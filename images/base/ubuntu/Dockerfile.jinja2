FROM ubuntu:24.04
RUN apt-get update -y \
    && apt-get upgrade -y \
    && apt-get install -y \
        ca-certificates \
        curl \
        git \
        gnupg \
        unzip \
        wget \
        zip \
    && update-ca-certificates \
    && rm -rf /var/lib/apt/lists/*
USER 1000
